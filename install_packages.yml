- name: playbook used to check if some packages are installed
  tasks:
    - name: check if wget and openssl is installed
      yum: 
        name: "{{ item }}"
        state: present
      with_items: 
        - openssl
        - wget

